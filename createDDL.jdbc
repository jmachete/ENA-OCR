CREATE TABLE APP.ZONE_DEFINITIONS (ID INTEGER GENERATED ALWAYS AS IDENTITY NOT NULL, ZONE_HEIGHT INTEGER, UPPERCASE CHAR(1), UPPERLEFTX INTEGER, LOWERCASE CHAR(1), ZONE_WITH INTEGER, NUMBER_TYPE CHAR(1), UPPERLEFTY INTEGER, TITLECASE CHAR(1), ZONE_DEF_NAME VARCHAR(40), ZONE_DEFINITIONS_ID INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE APP.ZONETYPE (ID INTEGER GENERATED ALWAYS AS IDENTITY NOT NULL, ZONE_TYPE_NAME VARCHAR(40), PRIMARY KEY (ID))
ALTER TABLE APP.ZONE_DEFINITIONS ADD CONSTRAINT ZNDFNTNSZNDFNTNSID FOREIGN KEY (ZONE_DEFINITIONS_ID) REFERENCES APP.ZONETYPE (ID)
